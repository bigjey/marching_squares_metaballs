{
  "devDependencies": {
    "cssnano": "^5.0.6",
    "postcss": "^8.3.5",
    "typescript": "^4.3.5"
  },
  "dependencies": {
    "parcel": "^2.0.0-beta.3.1"
  },
  "scripts": {
    "dev": "parcel serve index.html --dist-dir tmp",
    "build": "parcel build index.html --public-url='./' --dist-dir build"
  }
}